<h1>Resque Queue Status</h1>

<table border="1">
  <thead>
    <tr>
      <th>Queue Name</th>
      <th>Pending</th>
      <th>Processed</th>
      <th>Failed</th>
      <th>Last Executed</th>
      <th>Workers</th>
    </tr>
  </thead>
  <tbody>
    <% @queues.each do |queue| %>
      <tr>
        <td><%= queue[:name] %></td>
        <td><%= queue[:pending] %></td>
        <td><%= queue[:processed] %></td>
        <td><%= queue[:failed] %></td>
        <td><%= queue[:last_executed] || 'N/A' %></td>
        <td><%= queue[:workers] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
